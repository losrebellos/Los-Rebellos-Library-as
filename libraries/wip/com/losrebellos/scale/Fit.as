package com.losrebellos.scale {	import flash.geom.Rectangle;	/*	 *	 * @author los rebellos	 *	 */	public final class Fit 	{		////////////////////////////////////////////////////////////////////////////////////////////////////////////////
		// GET THE RECTANGLE TO FIT		//		// rectObject is the object area		// rectResize is the target area
		////////////////////////////////////////////////////////////////////////////////////////////////////////////////		public static function getFittedRectangle(fitType:String, rectObject:Rectangle, rectResize:Rectangle):Rectangle		{			var result:Rectangle = new Rectangle;			var rectObjectRatio:Number = rectObject.width / rectObject.height;			var rectResizeRatio:Number = rectResize.width / rectResize.height;						// fit inside			if(fitType == FitType.INSIDE)			{				if(rectResizeRatio > rectObjectRatio)				{					result.width = rectResize.height * rectObjectRatio;					result.height = rectResize.height;				}				else				{					result.width = rectResize.width;					result.height = rectResize.width / rectObjectRatio;				}			}						// fit inside outside			else if(fitType == FitType.OUTSIDE)			{				if(rectResizeRatio > rectObjectRatio)				{					result.width = rectResize.width;					result.height = rectResize.width / rectObjectRatio;				}				else				{					result.width = rectResize.height * rectObjectRatio;					result.height = rectResize.height;				}			}						// fit width			else if(fitType == FitType.WIDTH)			{				result.width = rectResize.width;				result.height = rectResize.width / rectObjectRatio;			}						// fit height			else if(fitType == FitType.HEIGHT)			{				result.width = rectResize.height * rectObjectRatio;				result.height = rectResize.height;			}						// result			return result;		}	}}